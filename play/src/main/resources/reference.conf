xingu {
  authenticator {
    enabled       = true
    secure        = false
    origins       = ["localhost"]
    cookie-prefix = "xingu-"
    header-prefix = "X-Xingu-"
    paths {
      allowed = ["/login", "/logout", "/i/ping"]
    }
    default-credentials = ""
  }
}
